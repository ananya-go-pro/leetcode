class Solution:
    def singleNumber(self, nums: List[int]) -> int:    
        if len(nums)==1:
            for i in nums:
                return i
        else: 
            a=0
            for i in nums:
                a^=i
            return a
