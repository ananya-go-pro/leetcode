class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i=0
        for k in nums:
            if i<1 or k>nums[i-1]:
                nums[i]=k
                i+=1
        return i
