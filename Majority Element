class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        a=set(nums)
        if len(a)==1:
            return list(a)[0]
        else:
            for i in list(a):
                if nums.count(i)>len(nums)/2:
                    return i
